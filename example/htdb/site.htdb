#include	config
#define	prefix	results
#define	title(str=Hello) <title>${str}</title>
#define	welcome <b>Hello world!</b>
#define index.html
	${title()}
	${morse(hello world)}
#	${sql(${prefix}:select * from stock.holding where symbol='DUK')}
#	${js({ this.setval('hello', 'world'); console.log("HELLO") })}
#	**${hello}**
	**${results[49]->account}**
	${welcome}
#live	if (getval(prefix) == resu)
	have results
#live	else
	no results
#live	endif
